@import "twitter/bootstrap/bootstrap";
@import "twitter/bootstrap/responsive";

// Set the correct sprite paths
@iconSpritePath: url(<%=Rails.configuration.path_prefix %>/assets/twitter/bootstrap/glyphicons-halflings.png);
@iconWhiteSpritePath: url(<%=Rails.configuration.path_prefix %>/assets/twitter/bootstrap/glyphicons-halflings-white.png);

@import "twitter/bootstrap/sprites.less";

body { padding-top: 60px; }
body .modal {
    width: 800px;
    margin-left: -400px;
}

@baseFontSize: 15px;

