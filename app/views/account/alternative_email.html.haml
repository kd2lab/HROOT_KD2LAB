= content_for :breadcrumb do
  %ul.breadcrumb
    %li
      =link_to "Login-Bereich", :controller => "account", :action => "index"
      %span.divider
        &gt;
    %li.active
      Account-Optionen

%h3 Account-Optionen

= render :partial => "tabs", :locals => {:selected => "Alternative E-Mail"} 

%h3 Alternative E-Mail-Adresse

- if !current_user.secondary_email.blank?
  - if current_user.secondary_email_confirmed_at
    .success
      
      Ihre alternative E-Mail-Adresse 
      %br
      %br
      %b= current_user.secondary_email
      %br
      %br
      wurde bestätigt und wird jetzt verwendet.
      %br
      %br
      = button_to "Alternative E-Mail löschen", {:delete => 1}, :class => "btn btn-primary", :confirm => "Sind Sie sicher, dass Sie Ihre alternative E-Mail-Adresse löschen möchten?"
      %br
  - else
    .notice
      Ihre alternative E-Mail-Adresse 
      %br
      %br
      %b= current_user.secondary_email
      %br
      %br
      ist derzeit noch nicht bestätigt.
      Es wurde Ihnen eine E-Mail mit einem Link zur Bestätigung Ihrer alternativen E-Mail-Adresse zugesendet.
      %br
      %br
      = link_to "Alternative E-Mail löschen", {:delete => 1}, :class => "btn btn-primary", :confirm => "Sind Sie sicher, dass Sie Ihre alternative E-Mail-Adresse löschen möchten?"
      = link_to "Bestätigungsemail erneut senden", {:resend => 1}, :class => "btn"
      %br
- else    
  = simple_form_for current_user, :html => {:class => "guarded_form form-horizontal"}, :url => '' do |f| 
    %br
    = f.input :secondary_email, :placeholder => 'name@domain.com'
    = f.submit "Alternative E-Mail speichern", :class => "btn btn-primary guarded_form_save"

%br
%br
