= content_for :breadcrumb do
  %ul.breadcrumb
    %li
      =link_to "Login-Bereich", :controller => "account", :action => "index"
      %span.divider
        &gt;
    %li.active
      Neuanmeldung zu Experiment-Sessions


%h3 Neuanmeldungen zu Experiment-Sessions

- if @availabe_sessions.count > 0
  Sie können sich für folgende Termine anmelden:
  %br
  %br

  = form_tag enroll_confirm_path(params[:code]) do
    
    %table.table
      - @availabe_sessions.each do |s|
        %tr
          %td
            = radio_button_tag "session", "#{s.id}" 
          %td
            %b= s.time_str
            = s.location.name if s.location
            
            - if s.following_sessions.count > 0
              %br
              - s.following_sessions.each do |f|  
                %b= f.time_str 
                =f.location.name if f.location
                %br
            %br
            
            - unless s.limitations.blank?
              %b
                Teilnahmehinweise: 
              %br
              = s.limitations
              %br
              %br
      
            - if s.following_sessions.count > 0
              .alert.alert-block
                %b Experiment mit Folgesessions:
                %br
                Dieses Experiment hat mehrere Termine, bitte überprüfen Sie, ob Sie an 
                %b allen
                angezeigten Terminen Zeit haben.
                %br
      
    = submit_tag 'Zu den gewählten Sessions anmelden', :class => "btn btn-primary"
    
    
- else    
  Derzeit können Sie sich für keine weiteren Sessions anmelden.