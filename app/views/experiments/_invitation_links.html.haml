Aktuellen Text speichern
%br
- current_user.settings.invitations.keys.sort.each do |name|
  =  link_to name, "", :class => 'save_invitation', :'data-name' => name
  %br
  
= link_to "Neu...", "", :class => 'save_invitation'
%br

- if current_user.settings.invitations.count > 0
  %br
  Einfügen
  %br
  - current_user.settings.invitations.keys.sort.each do |name|
    = link_to name, "", :class => 'load_invitation', :'data-name' => name
    %br

  %br
  Löschen
  %br
  - current_user.settings.invitations.keys.sort.each do |name|
    = link_to name, "", :class => 'delete_invitation', :'data-name' => name
    %br
  
