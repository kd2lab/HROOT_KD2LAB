= content_for :breadcrumb do
  = render :partial => "layouts/crumb", :object => [           |
      link_to("Experimente", experiments_path),                |
      link_to(@experiment.name, experiment_path(@experiment)), |
      "Sessions"]


= render :partial => "experiments/enrollment"
  
  
#dialog-form
  = form_tag send_messages_experiment_session_path, {:id => "dialog_form"} do
    %h2 Nachricht senden
    %br
    %b An:
    %br

    Alle Session-Teilnehmer
    %br
    %br
    %b Betreff:
    %br
    = text_field_tag :subject, "Experiment-Session am "+ @session.time_str, :class => "span8"  
    %br
    %br

    .templated-field-header.span8
      = render :partial => 'shared/text_templates', :locals => {:data => current_user.settings.templates, :element_id => 'message' }
      %b Text
    %br
    = text_area_tag :message, '', :rows => "14", :class => "span8"    

%h1
  = @experiment.name  


= render :partial => "experiments/tabs", :locals => {:selected => "Sessions"}


= link_to "Zurück zur Session-Übersicht", experiment_sessions_path(@experiment), :class => "btn"
%br

= form_tag participants_experiment_session_path(@experiment, @session), :method => 'post', :id => "participant_search" do
  #session_participants
    = render 'session_participants'